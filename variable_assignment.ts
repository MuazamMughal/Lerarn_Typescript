/* there are two techniques of variable type assignment is ts
one is explicit variable type assignment and second one is implicit
variable type assingnment*/

// the first one Explicit type is given below
let myName : string ="Muazam Mughal";
console.log(myName);
//in Explicit type we declear data type specfically


// Now the second type is Implicit type assignment

let myname ="Muazam Mughal";
console.log(myname)

//in implicit type we are not specifies the type of variable and
//lend it to ts to judge type own its own
